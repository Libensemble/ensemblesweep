
ensemblesweep
=============

ensemblesweep is a thin wrapper around [libEnsemble](https://libensemble.readthedocs.io/en/latest/) 
that provides a simple interface for running parallel objective-function or simulator evaluations.